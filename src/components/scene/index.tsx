"use client";
import React from 'react';
import Character from '../character';

const Scene = (props) => {
  return (
    <div>
      <Character />
    </div>
  )
}

export default Scene;
